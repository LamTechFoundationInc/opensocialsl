{%
  set classes = [
    'region--' ~ region|clean_class,
    'container',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes).setAttribute('id', 'content_top_cta') }}>
    {{ content }}
  </div>
{% endif %}
